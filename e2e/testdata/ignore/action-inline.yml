actions:
  - name: action.yml
    content: |
      runs:
        using: composite
        steps:
          - uses: actions/checkout@v6 # ghasec-ignore:unpinned-action
            with:
              persist-credentials: false

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
