workflows:
  - name: expression-in-permissions.yml
    content: |
      on: push
      permissions: ${{ github.actor }}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          permissions: ${{ github.actor }}
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/expression-in-permissions.yml:2:14
    ...
    2 | permissions: {{"${{ github.actor }}"}}
      |              ^^^^^^^^^^^^^^^^^^^ "permissions" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    --> {{.Dir}}/expression-in-permissions.yml:7:18
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     permissions: {{"${{ github.actor }}"}}
      |                  ^^^^^^^^^^^^^^^^^^^ "permissions" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    2 errors found in 1 file
