workflows:
  - name: workflow-run-workflows-type-mismatch.yml
    content: |
      on:
        workflow_run:
          workflows: build
          types: [completed]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow-run-workflows-type-mismatch.yml:3:16
    1 | on:
    2 |   workflow_run:
    3 |     workflows: build
      |                ^^^^^ "workflows" must be a sequence, but got string (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
