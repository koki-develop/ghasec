workflows:
  - name: choice-default-expression.yml
    content: |
      on:
        workflow_dispatch:
          inputs:
            env:
              type: choice
              description: target environment
              options: [dev, staging, prod]
              default: ${{ vars.DEFAULT_ENV }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/choice-default-expression.yml:8:18
    1 | on:
    2 |   workflow_dispatch:
    3 |     inputs:
    4 |       env:
    ...
    8 |         default: {{"${{ vars.DEFAULT_ENV }}"}}
      |                  ^^^^^^^^^^^^^^^^^^^^^^^ "default" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
