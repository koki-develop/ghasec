workflows:
  - name: filter-negation-multiple-filters.yml
    content: |
      on:
        push:
          branches:
            - main
          paths:
            - '!docs/**'
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/filter-negation-multiple-filters.yml:5:5
    1 | on:
    2 |   push:
    ...
    5 |     paths:
      |     ^^^^^ if a "paths" pattern starts with "!", at least one positive pattern is also required (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
