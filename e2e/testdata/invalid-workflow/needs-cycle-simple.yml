workflows:
  - name: needs-cycle-simple.yml
    content: |
      on: push
      jobs:
        a:
          needs: b
          runs-on: ubuntu-latest
          steps:
            - run: echo a
        b:
          needs: a
          runs-on: ubuntu-latest
          steps:
            - run: echo b

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/needs-cycle-simple.yml:4:5
    ...
    2 | jobs:
    3 |   a:
    4 |     needs: b
      |     ^^^^^ jobs must not have circular dependencies: a -> b -> a (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
