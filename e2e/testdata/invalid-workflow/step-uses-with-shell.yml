workflows:
  - name: step-uses-with-shell.yml
    content: |
      on: push
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
              shell: bash

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/step-uses-with-shell.yml:7:9
    ...
    2 | jobs:
    3 |   build:
    ...
    5 |     steps:
    6 |       - uses: actions/checkout@v4
    7 |         shell: bash
      |         ^^^^^ "shell" must be used with "run" (invalid-workflow)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
