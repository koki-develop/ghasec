workflows:
  - name: expression-in-on-config.yml
    content: |
      on:
        schedule:
          - cron: ${{ vars.CRON }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/expression-in-on-config.yml:3:13
    1 | on:
    2 |   schedule:
    3 |     - cron: {{"${{ vars.CRON }}"}}
      |             ^^^^^^^^^^^^^^^^ "cron" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
