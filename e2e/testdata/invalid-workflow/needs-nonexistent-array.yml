workflows:
  - name: needs-nonexistent-array.yml
    content: |
      on: push
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo build
        deploy:
          needs: [build, nonexistent]
          runs-on: ubuntu-latest
          steps:
            - run: echo deploy

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/needs-nonexistent-array.yml:8:20
    ...
    2 | jobs:
    ...
    7 |   deploy:
    8 |     needs: [build, nonexistent]
      |                    ^^^^^^^^^^^ job "deploy" needs nonexistent job "nonexistent" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
