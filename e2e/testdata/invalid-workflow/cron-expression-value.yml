workflows:
  - name: cron-expression-value.yml
    content: |
      on:
        schedule:
          - cron: ${{ vars.CRON_SCHEDULE }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
