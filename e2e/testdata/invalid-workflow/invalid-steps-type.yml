workflows:
  - name: invalid-steps-type.yml
    content: |
      on: push
      jobs:
        build:
          runs-on: ubuntu-latest
          steps: "not a sequence"

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/invalid-steps-type.yml:5:13
    ...
    2 | jobs:
    3 |   build:
    ...
    5 |     steps: "not a sequence"
      |             ^^^^^^^^^^^^^^ "steps" must be a sequence, but got string (invalid-workflow)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
