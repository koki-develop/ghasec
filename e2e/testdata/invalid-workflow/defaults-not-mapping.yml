workflows:
  - name: defaults-not-mapping.yml
    content: |
      on: push
      defaults: notamap
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/defaults-not-mapping.yml:2:11
    ...
    2 | defaults: notamap
      |           ^^^^^^^ "defaults" must be a mapping, but got String (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
