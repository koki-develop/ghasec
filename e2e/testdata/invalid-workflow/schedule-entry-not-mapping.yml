workflows:
  - name: schedule-entry-not-mapping.yml
    content: |
      on:
        schedule:
          - "0 0 * * *"
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/schedule-entry-not-mapping.yml:3:8
    1 | on:
    2 |   schedule:
    3 |     - "0 0 * * *"
      |        ^^^^^^^^^ "schedule" elements must be mappings, but got string (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
