workflows:
  - name: workflow-dispatch-input-unknown-key.yml
    content: |
      on:
        workflow_dispatch:
          inputs:
            myinput:
              description: test
              unknown_prop: value
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow-dispatch-input-unknown-key.yml:6:9
    1 | on:
    2 |   workflow_dispatch:
    3 |     inputs:
    4 |       myinput:
    ...
    6 |         unknown_prop: value
      |         ^^^^^^^^^^^^ unknown key "unknown_prop" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
