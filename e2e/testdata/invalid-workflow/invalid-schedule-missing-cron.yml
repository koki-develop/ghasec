workflows:
  - name: invalid-schedule-missing-cron.yml
    content: |
      on:
        schedule:
          - foo: bar
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/invalid-schedule-missing-cron.yml:3:10
    1 | on:
    2 |   schedule:
    3 |     - foo: bar
      |          ^ "schedule" element must have a "cron" key (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
