workflows:
  - name: expression-in-workflow-call-secrets.yml
    content: |
      on:
        workflow_call:
          secrets:
            MY_SECRET:
              required: ${{ github.event_name == 'push' }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/expression-in-workflow-call-secrets.yml:5:19
    1 | on:
    2 |   workflow_call:
    3 |     secrets:
    4 |       MY_SECRET:
    5 |         required: {{"${{ github.event_name == 'push' }}"}}
      |                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ "required" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
