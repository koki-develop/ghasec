workflows:
  - name: invalid-runs-on-mapping-key.yml
    content: |
      on: push
      jobs:
        build:
          runs-on:
            foo:
              bar:
                hoge:
                  baz:
          steps: []

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/invalid-runs-on-mapping-key.yml:5:7
    ...
    2 | jobs:
    3 |   build:
    4 |     runs-on:
    5 |       foo:
      |       ^^^ job "build" "runs-on" has unknown key "foo"; valid keys are "group" and "labels" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
