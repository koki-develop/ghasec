workflows:
  - name: needs-cycle-self.yml
    content: |
      on: push
      jobs:
        a:
          needs: a
          runs-on: ubuntu-latest
          steps:
            - run: echo a

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/needs-cycle-self.yml:4:5
    ...
    2 | jobs:
    3 |   a:
    4 |     needs: a
      |     ^^^^^ jobs must not have circular dependencies: a -> a (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
