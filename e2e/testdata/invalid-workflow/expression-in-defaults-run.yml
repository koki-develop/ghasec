workflows:
  - name: expression-in-defaults-run.yml
    content: |
      on: push
      permissions: {}
      defaults:
        run:
          shell: ${{ github.actor }}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          defaults:
            run:
              shell: bash
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/expression-in-defaults-run.yml:5:12
    ...
    3 | defaults:
    4 |   run:
    5 |     shell: {{"${{ github.actor }}"}}
      |            ^^^^^^^^^^^^^^^^^^^ "shell" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
