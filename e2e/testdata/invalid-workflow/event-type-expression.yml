workflows:
  - name: event-type-expression.yml
    content: |
      on:
        pull_request:
          types:
            - ${{ github.event.action }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/event-type-expression.yml:4:9
    1 | on:
    2 |   pull_request:
    3 |     types:
    4 |       - {{"${{ github.event.action }}"}}
      |         ^^^^^^^^^^^^^^^^^^^^^^^^^^ "types" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
