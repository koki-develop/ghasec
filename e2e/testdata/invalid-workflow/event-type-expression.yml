workflows:
  - name: event-type-expression.yml
    content: |
      on:
        pull_request:
          types:
            - ${{ github.event.action }}
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
