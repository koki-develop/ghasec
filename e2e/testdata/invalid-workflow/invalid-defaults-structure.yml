workflows:
  - name: invalid-defaults-structure.yml
    content: |
      on: push
      jobs:
        build:
          runs-on: ubuntu-latest
          defaults:
            foo: bar
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/invalid-defaults-structure.yml:6:7
    ...
    2 | jobs:
    3 |   build:
    ...
    5 |     defaults:
    6 |       foo: bar
      |       ^^^ "defaults" has unknown key "foo" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
