workflows:
  - name: job-only-env.yml
    content: |
      on: push
      jobs:
        build:
          env:
            FOO: bar

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/job-only-env.yml:4:8
    ...
    2 | jobs:
    3 |   build:
    4 |     env:
      |        ^ "runs-on" or "uses" is required (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
