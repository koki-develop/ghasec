workflows:
  - name: filter-negation-expression.yml
    content: |
      on:
        push:
          branches:
            - ${{ vars.BRANCH }}
            - '!main'
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/filter-negation-expression.yml:4:9
    1 | on:
    2 |   push:
    3 |     branches:
    4 |       - {{"${{ vars.BRANCH }}"}}
      |         ^^^^^^^^^^^^^^^^^^ "branches" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
