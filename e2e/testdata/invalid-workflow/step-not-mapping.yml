workflows:
  - name: step-not-mapping.yml
    content: |
      on: push
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - not a mapping

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/step-not-mapping.yml:6:9
    ...
    2 | jobs:
    3 |   build:
    ...
    5 |     steps:
    6 |       - not a mapping
      |         ^^^^^^^^^^^^^ "steps" elements must be mappings, but got string (invalid-workflow)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
