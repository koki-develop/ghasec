workflows:
  - name: workflow-run-workflows-non-string.yml
    content: |
      on:
        workflow_run:
          workflows: [Build, 42]
          types: [completed]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow-run-workflows-non-string.yml:3:24
    1 | on:
    2 |   workflow_run:
    3 |     workflows: [Build, 42]
      |                        ^^ "workflows" elements must be strings, but got integer (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
