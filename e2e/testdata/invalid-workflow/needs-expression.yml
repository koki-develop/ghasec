workflows:
  - name: needs-expression.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo build
        deploy:
          needs: ${{ fromJson(needs.build.outputs.matrix) }}
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo deploy

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/needs-expression.yml:10:12
    ...
     3 | jobs:
    ...
     9 |   deploy:
    10 |     needs: {{"${{ fromJson(needs.build.outputs.matrix) }}"}}
       |            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ "needs" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
