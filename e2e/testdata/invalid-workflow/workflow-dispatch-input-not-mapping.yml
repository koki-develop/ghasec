workflows:
  - name: workflow-dispatch-input-not-mapping.yml
    content: |
      on:
        workflow_dispatch:
          inputs:
            myinput: not-a-mapping
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow-dispatch-input-not-mapping.yml:4:16
    1 | on:
    2 |   workflow_dispatch:
    3 |     inputs:
    4 |       myinput: not-a-mapping
      |                ^^^^^^^^^^^^^ input "myinput" must be a mapping, but got String (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
