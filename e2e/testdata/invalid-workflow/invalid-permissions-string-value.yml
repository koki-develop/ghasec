workflows:
  - name: invalid-permissions-string-value.yml
    content: |
      on: push
      permissions: admin
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/invalid-permissions-string-value.yml:2:14
    ...
    2 | permissions: admin
      |              ^^^^^ "permissions" must be "read-all" or "write-all", but got "admin" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    1 error found in 1 file
