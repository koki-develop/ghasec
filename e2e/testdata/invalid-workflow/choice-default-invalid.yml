workflows:
  - name: choice-default-invalid.yml
    content: |
      on:
        workflow_dispatch:
          inputs:
            env:
              type: choice
              description: target environment
              options: [dev, staging, prod]
              default: production
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/choice-default-invalid.yml:8:18
    1 | on:
    2 |   workflow_dispatch:
    3 |     inputs:
    4 |       env:
    ...
    8 |         default: production
      |                  ^^^^^^^^^^ default value "production" must be one of the options: "dev", "staging", or "prod" (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 1 error found in 1 of 1 file
