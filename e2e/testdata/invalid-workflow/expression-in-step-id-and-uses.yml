workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - id: ${{ github.actor }}
              uses: ${{ github.actor }}

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:13
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - id: ${{"{{" }} github.actor {{"}}"}}
      |             ^^^^^^^^^^^^^^^^^^^ "id" must not contain expressions (invalid-workflow)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    --> {{.Dir}}/workflow.yml:9:15
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - id: ${{"{{" }} github.actor {{"}}"}}
    9 |         uses: ${{"{{" }} github.actor {{"}}"}}
      |               ^^^^^^^^^^^^^^^^^^^ "${{"{{" }} github.actor {{"}}"}}" must have a ref (invalid-workflow)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    --> {{.Dir}}/workflow.yml:9:15
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - id: ${{"{{" }} github.actor {{"}}"}}
    9 |         uses: ${{"{{" }} github.actor {{"}}"}}
      |               ^^^^^^^^^^^^^^^^^^^ "uses" must not contain expressions (invalid-workflow)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-workflow/README.md

    ✗ 3 errors found in 1 of 1 file
