actions:
  - name: action.yml
    content: |
      branding:
        color: pink
        icon: activity
      runs:
        using: composite
        steps:
          - run: echo hi
            shell: bash

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/action.yml:2:10
    1 | branding:
    2 |   color: pink
      |          ^^^^ "branding" has unknown color "pink" (invalid-action)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-action/README.md

    1 error found in 1 file
