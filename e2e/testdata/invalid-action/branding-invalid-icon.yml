actions:
  - name: action.yml
    content: |
      name: test
      description: test
      branding:
        color: blue
        icon: invalid-icon
      runs:
        using: composite
        steps:
          - run: echo hi
            shell: bash

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/action.yml:5:9
    ...
    3 | branding:
    ...
    5 |   icon: invalid-icon
      |         ^^^^^^^^^^^^ "branding" has unknown icon "invalid-icon" (invalid-action)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-action/README.md

    1 error found in 1 file
