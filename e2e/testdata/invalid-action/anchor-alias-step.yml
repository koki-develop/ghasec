actions:
  - name: action.yml
    content: |
      name: test
      description: test
      runs:
        using: composite
        steps:
          - &step
            run: echo hi
            shell: bash
          - *step

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
