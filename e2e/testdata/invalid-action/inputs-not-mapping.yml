actions:
  - name: action.yml
    content: |
      name: test
      description: test
      inputs: notamap
      runs:
        using: composite
        steps:
          - run: echo hi
            shell: bash

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/action.yml:3:9
    ...
    3 | inputs: notamap
      |         ^^^^^^^ "inputs" must be a mapping, but got String (invalid-action)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-action/README.md

    1 error found in 1 file
