workflows:
  - name: dangerous-checkout.yml
    content: |
      on:
        pull_request_target:
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
              with:
                persist-credentials: false
                ref: ${{ github.event.pull_request.head.sha }}

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/dangerous-checkout.yml:12:16
    ...
     4 | jobs:
     5 |   build:
    ...
     8 |     steps:
     9 |       - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
    10 |         with:
    ...
    12 |           ref: ${{"{{" }} github.event.pull_request.head.sha {{"}}"}}
       |                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ "ref" must not reference pull request head in a "pull_request_target" workflow (dangerous-checkout)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/dangerous-checkout/README.md

    ✗ 1 error found in 1 of 1 file
