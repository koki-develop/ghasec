workflows:
  - name: default-permissions-non-empty.yml
    content: |
      on: push
      permissions: read-all
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/default-permissions-non-empty.yml:2:1
    1 | on: push
    2 | permissions: read-all
      | ^^^^^^^^^^^ workflow-level "permissions" must be set to {}; grant permissions per job instead (default-permissions)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/default-permissions/README.md

    1 error found in 1 file
