workflows:
  - name: default-permissions-non-empty-mapping.yml
    content: |
      on: push
      permissions:
        contents: read
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/default-permissions-non-empty-mapping.yml:2:1
    ...
    2 | permissions:
      | ^^^^^^^^^^^ "permissions" must be {}; grant permissions per job instead (default-permissions)
    3 |   contents: read
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/default-permissions/README.md

    ✗ 1 error found in 1 of 1 file
