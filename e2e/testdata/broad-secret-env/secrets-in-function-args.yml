workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          env:
            CREDS: ${{ format('{0}:{1}', secrets.USER, secrets.PASS) }}
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:36
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     env:
    8 |       CREDS: {{"${{ format('{0}:{1}', secrets.USER, secrets.PASS) }}"}}
      |                                    ^^^^^^^^^^^^ secrets must not be set in job-level environment variables; set them in step-level environment variables instead (broad-secret-env)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/broad-secret-env/README.md

    --> {{.Dir}}/workflow.yml:8:50
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     env:
    8 |       CREDS: {{"${{ format('{0}:{1}', secrets.USER, secrets.PASS) }}"}}
      |                                                  ^^^^^^^^^^^^ secrets must not be set in job-level environment variables; set them in step-level environment variables instead (broad-secret-env)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/broad-secret-env/README.md

    ✗ 2 errors found in 1 of 1 file
