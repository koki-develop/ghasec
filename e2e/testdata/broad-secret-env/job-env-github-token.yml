workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          env:
            GH_TOKEN: ${{ github.token }}
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:21
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     env:
    8 |       GH_TOKEN: {{"${{ github.token }}"}}
      |                     ^^^^^^^^^^^^ github.token must not be set in job-level environment variables; set them in step-level environment variables instead (broad-secret-env)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/broad-secret-env/README.md

    ✗ 1 error found in 1 of 1 file
