workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build: &build
          runs-on: ubuntu-latest
          timeout-minutes: 10
          env:
            TOKEN: ${{ secrets.TOKEN }}
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:18
    ...
    3 | jobs:
    4 |   build: &build
    ...
    7 |     env:
    8 |       TOKEN: {{"${{ secrets.TOKEN }}"}}
      |                  ^^^^^^^^^^^^^ secrets must not be set in job-level environment variables; set them in step-level environment variables instead (broad-secret-env)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/broad-secret-env/README.md

    ✗ 1 error found in 1 of 1 file
