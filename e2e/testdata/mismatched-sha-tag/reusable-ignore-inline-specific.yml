args: --online
workflows:
  - name: reusable-ignore-inline-specific.yml
    content: |
      on: push
      permissions: {}
      jobs:
        call:
          # v1 hint via separate ignore directive on next line; using "v1" inline alone would resolve the comment as the tag
          # ghasec-ignore:mismatched-sha-tag
          uses: octo-org/reusable-repo/.github/workflows/ci.yml@3333333333333333333333333333333333333333 # v1

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    ✓ 1 file checked, no errors found
