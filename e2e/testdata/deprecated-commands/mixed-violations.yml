workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      env:
        ACTIONS_ALLOW_UNSECURE_COMMANDS: true
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo "::set-env name=FOO::bar"

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:4:3
    ...
    3 | env:
    4 |   ACTIONS_ALLOW_UNSECURE_COMMANDS: true
      |   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ "ACTIONS_ALLOW_UNSECURE_COMMANDS" must not be enabled (deprecated-commands)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    --> {{.Dir}}/workflow.yml:10:20
    ...
     5 | jobs:
     6 |   build:
    ...
     9 |     steps:
    10 |       - run: echo "::set-env name=FOO::bar"
       |                    ^^^^^^^^^^^^^^^^^^^^^^^ deprecated workflow command "::set-env" must not be used (deprecated-commands)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    ✗ 2 errors found in 1 of 1 file
