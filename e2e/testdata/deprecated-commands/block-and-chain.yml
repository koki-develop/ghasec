workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: |
                foo && echo "::set-output name=x::y"
                echo done
expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:9:24
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: |
    9 |           foo && echo "::set-output name=x::y"
      |                        ^^^^^^^^^^^^^^^^^^^^^^ deprecated workflow command "::set-output" must not be used (deprecated-commands)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    --> {{.Dir}}/workflow.yml:10:16
    ...
     3 | jobs:
     4 |   build:
    ...
     7 |     steps:
     8 |       - run: |
    ...
    10 |           echo done
       |                ^ Use semicolon or linefeed before 'done' (or quote to make it literal). (shellcheck/SC1010)
      Ref: https://www.shellcheck.net/wiki/SC1010

    ✗ 2 errors found in 1 of 1 file
