workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo "FOO=bar" >> $GITHUB_ENV
expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:32
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: echo "FOO=bar" >> $GITHUB_ENV
      |                                ^^^^^^^^^^^ Double quote to prevent globbing and word splitting. (shellcheck/SC2086)
      Ref: https://www.shellcheck.net/wiki/SC2086

    ✗ 1 error found in 1 of 1 file
