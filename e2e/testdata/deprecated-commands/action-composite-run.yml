actions:
  - name: action.yml
    content: |
      name: My Action
      runs:
        using: composite
        steps:
          - run: echo "::set-env name=FOO::bar"
            shell: bash

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/action.yml:5:18
    ...
    2 | runs:
    ...
    4 |   steps:
    5 |     - run: echo "::set-env name=FOO::bar"
      |                  ^^^^^^^^^^^^^^^^^^^^^^^ deprecated workflow command "::set-env" must not be used (deprecated-commands)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    ✗ 1 error found in 1 of 1 file
