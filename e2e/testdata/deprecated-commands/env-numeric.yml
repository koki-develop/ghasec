workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      env:
        ACTIONS_ALLOW_UNSECURE_COMMANDS: 1
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hello

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    ✓ 1 file checked, no errors found
