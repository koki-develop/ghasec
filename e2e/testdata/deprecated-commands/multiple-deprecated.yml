workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: |
                echo "::set-env name=A::1"
                echo "::add-path::/opt"

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:9:17
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: |
    9 |           echo "::set-env name=A::1"
      |                 ^^^^^^^^^^^^^^^^^^^ deprecated workflow command "::set-env" must not be used (deprecated-commands)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    --> {{.Dir}}/workflow.yml:10:17
    ...
     3 | jobs:
     4 |   build:
    ...
     7 |     steps:
     8 |       - run: |
    ...
    10 |           echo "::add-path::/opt"
       |                 ^^^^^^^^^^^^^^^^ deprecated workflow command "::add-path" must not be used (deprecated-commands)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/deprecated-commands/README.md

    ✗ 2 errors found in 1 of 1 file
