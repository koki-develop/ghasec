workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: docker://alpine@sha256:abcdef

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:15
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - uses: docker://alpine@sha256:abcdef
      |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ "docker://alpine@sha256:abcdef" must be pinned to a digest (unpinned-action)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/unpinned-action/README.md

    ✗ 1 error found in 1 of 1 file
