workflows:
  - name: multiple-unpinned-actions.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v6
              with:
                persist-credentials: false
            - uses: actions/setup-go@v5
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/multiple-unpinned-actions.yml:7:32
    ...
    3 | jobs:
    4 |   build:
    ...
    6 |     steps:
    7 |       - uses: actions/checkout@v6
      |                                ^^ "actions/checkout@v6" must be pinned to a full length commit SHA (unpinned-action)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/unpinned-action/README.md

    --> {{.Dir}}/multiple-unpinned-actions.yml:10:32
    ...
     3 | jobs:
     4 |   build:
    ...
     6 |     steps:
    ...
    10 |       - uses: actions/setup-go@v5
       |                                ^^ "actions/setup-go@v5" must be pinned to a full length commit SHA (unpinned-action)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/unpinned-action/README.md

    2 errors found in 1 file
