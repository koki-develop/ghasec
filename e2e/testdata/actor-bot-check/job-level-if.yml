workflows:
  - name: job-if.yml
    content: |
      on: pull_request
      permissions: {}
      jobs:
        build:
          if: github.actor == 'dependabot[bot]'
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo hi

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/job-if.yml:5:9
    ...
    3 | jobs:
    4 |   build:
    5 |     if: github.actor == 'dependabot[bot]'
      |         ^^^^^^^^^^^^ "if" must not use "github.actor" to check for bots in a "pull_request" workflow (actor-bot-check)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/actor-bot-check/README.md

    ✗ 1 error found in 1 of 1 file
