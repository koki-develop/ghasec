workflows:
  - name: valid-docker-action.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: docker://alpine:3.8

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
