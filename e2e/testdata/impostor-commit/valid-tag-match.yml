workflows:
  - name: valid-tag-match.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: actions/checkout@bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb # v4
              with:
                persist-credentials: false

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
