workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: actions/github-script@v7 # ghasec-ignore:unpinned-action
              with:
                script: console.log('${{ github.token }}')

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:10:32
    ...
     3 | jobs:
     4 |   build:
    ...
     7 |     steps:
     8 |       - uses: actions/github-script@v7 # ghasec-ignore:unpinned-action
     9 |         with:
    10 |           script: console.log('{{"${{ github.token }}"}}')
       |                                ^^^^^^^^^^^^^^^^^^^ "script" must not contain expressions; use environment variables instead (script-injection)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/script-injection/README.md

    1 error found in 1 file
