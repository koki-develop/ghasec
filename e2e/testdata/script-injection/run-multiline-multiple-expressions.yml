workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: |
                echo ${{ github.actor }}
                echo hello
                echo ${{ github.token }}

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:9:16
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: |
    9 |           echo {{"${{ github.actor }}"}}
      |                ^^^^^^^^^^^^^^^^^^^ "run" must not contain expressions; use environment variables instead (script-injection)
    ...
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/script-injection/README.md

    --> {{.Dir}}/workflow.yml:11:16
    ...
     3 | jobs:
     4 |   build:
    ...
     7 |     steps:
     8 |       - run: |
    ...
    11 |           echo {{"${{ github.token }}"}}
       |                ^^^^^^^^^^^^^^^^^^^ "run" must not contain expressions; use environment variables instead (script-injection)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/script-injection/README.md

    2 errors found in 1 file
