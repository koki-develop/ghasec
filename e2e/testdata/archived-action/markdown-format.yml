args: --online --format markdown
workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            # ghasec-ignore:unpinned-action
            - uses: archived-org/archived-repo@v1

expected:
  exit_code: 1
  stdout: |
    ## {{.Dir}}/workflow.yml:9:15

    ```yaml
          - uses: archived-org/archived-repo@v1
    ```

    - **Rule**: archived-action
    - **Message**: "archived-org/archived-repo" is archived and must not be used
    - **Why**: Archived repositories no longer receive security patches, bug fixes, or dependency updates. Using them exposes workflows to known and future vulnerabilities that will never be addressed
    - **Fix**: Migrate to an actively maintained alternative action that provides the same functionality
    - **Ref**: https://github.com/koki-develop/ghasec/blob/main/rules/archived-action/README.md

    ---

    1 error found in 1 of 1 file.
  stderr: ""
