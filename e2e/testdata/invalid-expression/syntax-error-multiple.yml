workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: ${{ == }} and ${{ == }} # ghasec-ignore:script-injection

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:14
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: ${{"{{" }} == {{"}}"}} and ${{"{{" }} == {{"}}"}} # ghasec-ignore:script-injection
      |              ^^^^^^^^^ invalid expression syntax: expected expression, but got '==' (invalid-expression)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-expression/README.md

    --> {{.Dir}}/workflow.yml:8:28
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: ${{"{{" }} == {{"}}"}} and ${{"{{" }} == {{"}}"}} # ghasec-ignore:script-injection
      |                            ^^^^^^^^^ invalid expression syntax: expected expression, but got '==' (invalid-expression)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-expression/README.md

    ✗ 2 errors found in 1 of 1 file
