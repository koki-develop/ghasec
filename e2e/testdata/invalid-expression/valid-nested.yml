workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: echo ${{ contains(format('{0}', 'hello'), 'ell') }}
            - run: echo ${{ (true && (false || true)) }}

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
