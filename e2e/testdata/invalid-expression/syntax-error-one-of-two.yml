workflows:
  - name: workflow.yml
    content: |
      on: push
      permissions: {}
      jobs:
        build:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - run: ${{ github.actor }} and ${{ == }} # ghasec-ignore:script-injection

expected:
  exit_code: 1
  stdout: ""
  stderr: |
    --> {{.Dir}}/workflow.yml:8:38
    ...
    3 | jobs:
    4 |   build:
    ...
    7 |     steps:
    8 |       - run: {{"${{ github.actor }}"}} and {{"${{ == }}"}} # ghasec-ignore:script-injection
      |                                      ^^^^^^^^^ invalid expression syntax: expected expression, but got '==' (invalid-expression)
      Ref: https://github.com/koki-develop/ghasec/blob/main/rules/invalid-expression/README.md

    ✗ 1 error found in 1 of 1 file
