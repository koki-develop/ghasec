actions:
  - name: action.yml
    content: |
      name: Test
      description: A test action
      runs:
        using: composite
        steps:
          - run: echo ${{ github.actor }}
            shell: bash

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
