actions:
  - name: action.yml
    content: |
      name: Test
      description: A test action
      runs:
        using: composite
        steps:
          - run: echo ${{ github.actor }} # ghasec-ignore:script-injection
            shell: bash

expected:
  exit_code: 0
  stdout: ""
  stderr: |
    no errors found
